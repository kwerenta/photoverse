<h2 class="font-medium text-3xl mb-4"><%= (action_name == "index") ? "Feed" : "Explore" %></h2>
  <div class="columns-1 md:columns-2 xl:columns-3 gap-4">
    <% if @posts.blank? %>
      <p>You are not following anyone.</p>
    <% else %>
      <%= render @posts %>
    <% end %>
  </div>
